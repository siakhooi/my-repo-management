name: "Dependabot: python" 

on:
  schedule:
    - cron: '0 17 * * *'
    - cron: '0 5 * * *'
  workflow_dispatch:
    inputs: 
      action:
        description: Action to run
        required: true
        type: choice
        options: 
          - autoreview
          - automerge

jobs:
  auto-review:
    if: |
      (github.event_name == 'schedule' && github.event.schedule=='0 17 * * *') ||
      (github.event_name == 'workflow_dispatch' && inputs.action == 'autoreview')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - run: npm install -g @siakhooi/gh-pr
      - run: gh-pr autoreview --assigned-to-me --authored-by-dependabot --label dependencies --not-yet-reviewed --limit 10 --label python --max-update 5 --max-update-per-repo 1
        env:
          GITHUB_TOKEN: ${{ secrets.ALL_REPO_GITHUB_TOKEN }}

  auto-merge:
    if: |
      (github.event_name == 'schedule' && github.event.schedule=='0 5 * * *') ||
      (github.event_name == 'workflow_dispatch' && inputs.action == 'automerge')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - run: npm install -g @siakhooi/gh-pr
      - run: gh-pr automerge --assigned-to-me --authored-by-dependabot --label dependencies --limit 10 --label python --max-update 5 --max-update-per-repo 1
        env:
          GITHUB_TOKEN: ${{ secrets.ALL_REPO_GITHUB_TOKEN }}
